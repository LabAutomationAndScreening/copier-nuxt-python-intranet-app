# packages:  # TODO: move this to top level and change the mount for node_modules in devcontainer
#   - 'frontend'

ignoredBuiltDependencies:
  # managed by upstream template
  - esbuild        # ships pre-built platform binaries via optional deps; postinstall is redundant
  - unrs-resolver  # ships pre-built platform binaries via optional deps; postinstall is redundant
  - '@tailwindcss/oxide' # ships pre-built platform binaries via optional deps; postinstall is redundant
  - vue-demi # postinstall switches between Vue 2/3 mode; this is a Vue 3-only project so default is already correct
  - '@parcel/watcher' # ships pre-built platform binaries via optional deps; postinstall is redundant
  # specific to this repository


onlyBuiltDependencies:
  # managed by upstream template

  # specific to this repository
